# Backend base URLs — public, unauthenticated (ADR-08).
# Copy to .env for local development; set the same variables in Vercel for deploys.
# Never put secrets in VITE_* vars: Vite inlines them into the public bundle.
VITE_API_BASE_URL=https://wydokyegph.execute-api.eu-central-1.amazonaws.com
VITE_SSE_BASE_URL=https://vpjjdvoeej5izlqy3nnpllmyua0idsrp.lambda-url.eu-central-1.on.aws
